{
  "description": "Agent 1 cron registry - read on session restart.",
  "crons": [
    {
      "id": "keepalive",
      "name": "Keep-alive ping",
      "cron": "*/20 * * * *",
      "prompt": "Check channel connection is active.",
      "enabled": true
    }
  ]
}
